# Quarto build cache and rendered output (generated; never commit)
/.quarto/
/_site/
# NOTE: _freeze/ is intentionally NOT ignored here.
# It holds frozen notebook outputs that CI reuses instead of re-executing.

# OS / editor cruft
.DS_Store
.vscode/

**/*.quarto_ipynb
